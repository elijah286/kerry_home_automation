[build]
builder = "dockerfile"
dockerfilePath = "proxy/Dockerfile"

[deploy]
startCommand = "node dist/proxy/src/index.js"
healthcheckPath = "/health"
healthcheckTimeout = 10
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 5

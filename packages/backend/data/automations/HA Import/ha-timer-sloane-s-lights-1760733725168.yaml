id: ha-timer-sloane-s-lights-1760733725168
name: 'Timer: Sloane''s Lights'
group: HA Import
description: 'Imported from Home Assistant (id: 1760733725168).'
enabled: false
mode: single
triggers:
  - cron: '*/1 * * * *'
    type: time
  - type: device_state
    deviceId: light.sloane_s_lights
    attribute: state
  - type: device_state
    deviceId: input_select.system_mode
    attribute: state
conditions:
  - type: or
    conditions:
      - op: eq
        type: device_state
        value: 'on'
        deviceId: light.sloanes_bedroom_ceiling_fan_light
        attribute: state
      - op: eq
        type: device_state
        value: 'on'
        deviceId: light.sloanes_bedroom_closet_light
        attribute: state
      - op: eq
        type: device_state
        value: 'on'
        deviceId: light.sloanes_bedroom_bedside_lamp
        attribute: state
      - op: eq
        type: device_state
        value: 'on'
        deviceId: light.sloanes_bedroom_dresser_lamp
        attribute: state
actions:
  - else:
      - then:
          - type: device_command
            command:
              type: light
              action: turn_off
              deviceId: light.sloanes_bedroom_ceiling_fan_light
            deviceId: light.sloanes_bedroom_ceiling_fan_light
        type: condition
        condition:
          op: eq
          type: device_state
          value: 'on'
          deviceId: light.sloanes_bedroom_ceiling_fan_light
          attribute: state
      - then:
          - type: device_command
            command:
              type: light
              action: turn_off
              deviceId: light.sloanes_bedroom_closet_light
            deviceId: light.sloanes_bedroom_closet_light
        type: condition
        condition:
          op: eq
          type: device_state
          value: 'on'
          deviceId: light.sloanes_bedroom_closet_light
          attribute: state
    then:
      - type: device_command
        command:
          type: light
          action: turn_off
          deviceId: light.sloane_s_lights
        deviceId: light.sloane_s_lights
    type: condition
    condition:
      type: and
      conditions:
        - op: eq
          type: device_state
          value: 'on'
          deviceId: input_boolean.upstairs_vacant
          attribute: state
        - op: eq
          type: device_state
          value: 'on'
          deviceId: light.sloane_s_lights
          attribute: state

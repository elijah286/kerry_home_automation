id: ha-outdoors-exterior-sconces-turn-on-landscape-1569730871521
name: Outdoors Exterior sconces turn on landscape
group: HA Import
description: 'Imported from Home Assistant (id: 1569730871521).'
enabled: false
mode: single
triggers:
  - to: 'on'
    type: device_state
    deviceId: switch.exterior_sconces
    attribute: state
conditions:
  - type: not
    condition:
      type: and
      conditions:
        - op: eq
          type: device_state
          value: Critical
          deviceId: input_select.power_saving_mode
          attribute: state
        - op: eq
          type: device_state
          value: Aggressive
          deviceId: input_select.power_saving_mode
          attribute: state
actions:
  - type: device_command
    command:
      type: switch
      action: turn_on
      deviceId: switch.front_porch_landscape_side
    deviceId: switch.front_porch_landscape_side
